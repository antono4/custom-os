interface:
  display_name: "Custom Os"
  short_description: "Repo-specific patterns and workflows for custom-os"
  default_prompt: "Use the custom-os repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true