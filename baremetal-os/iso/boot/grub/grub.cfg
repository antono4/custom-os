# NanoOS GRUB Configuration
set timeout=5
set default=0

menuentry "NanoOS v0.1" {
    echo "Loading NanoOS..."
    multiboot /boot/kernel.bin
    boot
}

menuentry "NanoOS v0.1 (Debug Mode)" {
    echo "Loading NanoOS (Debug)..."
    multiboot /boot/kernel.bin debug
    boot
}
